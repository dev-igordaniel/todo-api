package com.todolist.todoapi.entity;

public enum TaskStatus {    //ENUM obriga valores fixos pré-definidos
    PENDING,                //TaskStatus só vai poder receber os valores que estão aqui dentro
    DONE
}
